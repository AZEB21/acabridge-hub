# services:
#   # ── Frontend (React SPA) ──────────────────────────────────────────────────
#   - type: web
#     name: acabridge-hub-2
#     runtime: static
#     rootDir: frontend
#     buildCommand: npm install && npm run build
#     staticPublishPath: build
#     pullRequestPreviewsEnabled: false
#     routes:
#       - type: rewrite
#         source: /*
#         destination: /index.html
